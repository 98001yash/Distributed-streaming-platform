apiVersion: v1
kind: Service
metadata:
  name: streaming-service
  namespace: streaming-platform

spec:
  selector:
    app: streaming-service

  ports:
    - port: 8095
      targetPort: 8095

  type: ClusterIP