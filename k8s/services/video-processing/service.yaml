apiVersion: v1
kind: Service
metadata:
  name: video-processing-service
  namespace: streaming-platform

spec:
  selector:
    app: video-processing-service

  ports:
    - port: 8094
      targetPort: 8094

  type: ClusterIP