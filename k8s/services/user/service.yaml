apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: streaming-platform

spec:
  selector:
    app: user-service

  ports:
    - port: 8091
      targetPort: 8091

  type: ClusterIP