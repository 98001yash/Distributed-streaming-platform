apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: streaming-platform

spec:
  selector:
    app: analytics-service

  ports:
    - port: 9095
      targetPort: 9095

  type: ClusterIP