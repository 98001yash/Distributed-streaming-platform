apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: mysql-pvc
  namespace: streaming-platform

spec:
  storageClassName: standard
  volumeName: mysql-pv

  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 5Gi